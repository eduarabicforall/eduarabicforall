# EduArabic for All — Environment Variables
# Copy this file to .env.local and fill in your values

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Cloudflare R2 gatekeeper Worker (video streaming)
VITE_R2_WORKER_URL=

# Payment gateways (used by Supabase Edge Functions, NOT exposed to client)
# Set these as Supabase Edge Function secrets, not here.
